language: node_js
before_script:
  - npm install -g gatsby
node_js:
  - "10"

script:
  - echo "skipping tests"

install: true
deploy:
  provider: script
  script: yarn install && yarn run deploy
  skip_cleanup: true
  on:
    branch: master